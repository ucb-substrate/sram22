---
title: Quickstart
description: Install SRAM22 and generate a macro from a TOML configuration.
---

import ConfigTable from "@site/src/components/ConfigTable";

## Install

With a current stable Rust toolchain and Cargo:

```bash
cargo install --git https://github.com/ucb-substrate/sram22 --locked sram22
```

## Generate an SRAM

Create `sram22.toml` in a working directory:

```toml
num_words = 64
data_width = 32
mux_ratio = 4
write_size = 8
```

Run the generator:

```bash
sram22
```

SRAM22 reads `sram22.toml` and writes GDS, LEF, SPICE, and Verilog to
`./sram22_64x32m4w8/`. To choose explicit paths:

```bash
sram22 --config sram22.toml --output-dir output --spice-corner tt
```

To simulate the macro in ngspice, include its `.spice` file in a testbench and
provide power supplies, stimuli, and a simulation temperature. When combining
several macros in one testbench, keep one copy of their shared model definitions.

## Command-line options

| Option | Meaning |
| --- | --- |
| `-c, --config <CONFIG>` | TOML path; default `sram22.toml` |
| `-o, --output-dir <OUTPUT_DIR>` | Output directory; default macro name |
| `--spice-corner <CORNER>` | SPICE model corner: `tt`, `ss`, or `ff`; default `tt` |
| `-h, --help` | Available options for the installed build |
| `-V, --version` | Version |

The BWRC build also offers `--lib`, `--drc`, `--lvs`, `--pex`, and
`--all`. These run licensed characterization or verification. `--lib` emits
corner-suffixed Liberty files, such as `sram22_64x32m4w8_tt_025C_1v80.lib`.
Liberate characterizes TT, SS, and FF. Set the temperature for SPICE simulations
in your testbench.

The [macro catalog](/docs/macros/) provides characterized views for download.

## Configuration reference

<ConfigTable />

## Placement

Use the project's four supported quarter-turn placements: R90, R270, and their
mirrors. Enforce this restriction in your flow; LEF symmetry alone does not enforce
it. See [Orientations](/docs/orientations/), then the
[OpenROAD](/docs/tutorial/openroad/) or [Cadence](/docs/tutorial/cadence/) integration outline.

## Advanced setup

See the README for [source builds and custom forks](https://github.com/ucb-substrate/sram22#local-checkout-or-custom-fork),
[BWRC installation](https://github.com/ucb-substrate/sram22#bwrc-installation),
and [external PDK overrides](https://github.com/ucb-substrate/sram22#external-open-pdk-override).
